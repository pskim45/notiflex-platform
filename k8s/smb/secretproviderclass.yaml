apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: notiflex-secrets
  namespace: notiflex
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/project-10edc337-9677-4dfc-91a/secrets/valkey-password/versions/latest"
        path: "valkey-password"
